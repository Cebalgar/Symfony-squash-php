{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            {{ form_start(userForm)}}
            {{ form_widget(userForm )}}
            <br>
            <button class="btn btn-dark" type="submit">Registrar</button>
            {{ form_end(userForm) }}
        </div>
    </div>


{% endblock %}

{% block title %}
    Nuevo Usuario
{% endblock %}
